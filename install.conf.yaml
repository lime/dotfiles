- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.aliases:
    ~/.config:
    ~/.zdir:
    ~/.zshrc:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
